@page "/admin/s2Handstore/nguoi-dung/gan-role/{id}"
@model MVC_Core.Areas.Admin.Pages.User.EditModel
@{
}
<h4>Thiết lập role cho user: @Model.user.UserName</h4>


@{


}
<div class="row">
    <div class="col-md-6">
        <form id="set-password-form" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label ></label>
                
                <span asp-validation-for="RoleNames" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary">Cập nhật</button>
            <a class="btn btn-primary" asp-page="./Index">Danh sách thành viên</a>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}